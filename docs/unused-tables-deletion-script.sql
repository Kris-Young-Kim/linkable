-- =========================================================
-- 사용하지 않는 테이블 삭제 스크립트
-- 작성일: 2025-01-XX
-- =========================================================
-- 
-- 주의: 이 스크립트는 현재 사용하지 않는 테이블을 삭제합니다.
-- 하지만 분석 결과, 모든 테이블이 사용 중이거나 구조적으로 필요합니다.
-- 
-- 따라서 이 스크립트는 실행하지 않아야 합니다.
-- 
-- 만약 향후 특정 테이블을 삭제해야 한다면, 아래 주석을 해제하고 실행하세요.
-- =========================================================

-- =========================================================
-- 삭제 가능한 테이블: 없음
-- =========================================================
-- 
-- 분석 결과:
-- - 모든 테이블이 실제 코드에서 사용 중이거나
-- - FK 제약조건으로 참조되거나
-- - 정규화 구조의 일부이거나
-- - 향후 사용 예정입니다.
-- 
-- 따라서 삭제할 테이블이 없습니다.
-- =========================================================

-- =========================================================
-- 참고: 빈 테이블이지만 필요한 이유
-- =========================================================

-- 1. manufacturers (0 rows)
--    - products.manufacturer_id FK로 참조됨
--    - 정규화 구조의 일부
--    - 삭제 불가 (FK 제약조건 위반)

-- 2. categories (0 rows)
--    - products.category_id FK로 참조됨
--    - 정규화 구조의 일부
--    - 삭제 불가 (FK 제약조건 위반)

-- 3. coupons (0 rows)
--    - lib/incentives.ts에서 사용 중
--    - app/api/incentives/coupons/route.ts에서 사용 중
--    - 삭제 불가 (코드에서 사용 중)

-- 4. user_coupons (0 rows)
--    - lib/incentives.ts에서 사용 중
--    - 삭제 불가 (코드에서 사용 중)

-- 5. consultation_icf_codes (0 rows)
--    - 정규화 구조의 일부
--    - ICF 코드 정규화를 위한 관계 테이블
--    - 삭제 불가 (정규화 구조 필요)

-- 6. ippa_evaluations (0 rows)
--    - app/api/ippa/route.ts에서 사용 중
--    - 삭제 불가 (코드에서 사용 중)

-- =========================================================
-- 만약 향후 특정 테이블을 삭제해야 한다면
-- =========================================================
-- 
-- 아래 주석을 해제하고 실행하세요.
-- 단, FK 제약조건이 있는 경우 먼저 제약조건을 제거해야 합니다.
-- 
-- BEGIN;
-- 
-- -- 예시: manufacturers 테이블 삭제 (FK 제약조건 제거 후)
-- -- ALTER TABLE products DROP CONSTRAINT IF EXISTS fk_products_manufacturer;
-- -- DROP TABLE IF EXISTS manufacturers CASCADE;
-- 
-- -- 예시: categories 테이블 삭제 (FK 제약조건 제거 후)
-- -- ALTER TABLE products DROP CONSTRAINT IF EXISTS fk_products_category;
-- -- DROP TABLE IF EXISTS categories CASCADE;
-- 
-- COMMIT;
-- 
-- =========================================================
